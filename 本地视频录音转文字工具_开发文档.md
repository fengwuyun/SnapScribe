# 本地视频 / 录音转文字工具开发文档

> 技术栈：Tauri 2 + React + TypeScript + Tailwind CSS + shadcn/ui + Lucide Icons + FFmpeg + sherpa-onnx + SenseVoiceSmall INT8  
> 目标平台：Windows  
> 产品定位：本地、轻量、零配置、点击即用的视频 / 录音转文字桌面工具  
> UI 约束来源：`UI_DESIGN_SPEC.md`

---

# 1. 产品目标

开发一个 Windows 本地视频 / 录音转文字工具。

核心目标：

- 简单易用
- 界面现代、统一、美观
- 安装后直接使用
- 完全本地运行
- 无需登录
- 无需 API Key
- 无需下载模型
- 无需安装 Python、FFmpeg 或额外运行环境
- 尽量减少后续维护成本

第一版只解决一个问题：

> 把视频或录音拖入软件，自动转换成文字，并支持复制和导出。

不追求功能数量，优先保证：

```text
低学习成本
+
低维护成本
+
稳定的本地转写
+
舒服的阅读和导出体验
```

---

# 2. 产品边界

## 2.1 第一版必须实现

| 功能 | 优先级 |
|---|---|
| 拖拽导入文件 | P0 |
| 点击选择文件 | P0 |
| MP4 / MOV / MKV / MP3 / M4A / WAV 支持 | P0 |
| 文件信息展示 | P0 |
| 开始转写 | P0 |
| 转写进度展示 | P0 |
| 转写结果展示 | P0 |
| 时间戳展示 | P0 |
| 点击时间戳跳转播放位置 | P1 |
| 播放 / 暂停 | P1 |
| 复制全文 | P0 |
| 导出 TXT | P0 |
| 导出 SRT | P0 |
| 取消转写 | P1 |
| 错误提示 | P0 |

## 2.2 第一版明确不做

- 登录 / 注册
- 云同步
- 在线 API
- AI 摘要
- AI 润色
- 翻译
- 说话人分离
- 实时录音转写
- 多模型切换
- 模型下载
- 模型管理
- 自动更新
- 项目管理
- 工作空间
- 数据库
- 账号体系
- 团队协作

原则：

> 不因为“以后可能需要”提前引入复杂架构。

---

# 3. 技术架构

```text
用户
  ↓
React UI
  ↓
Windows WebView2
  ↓
Tauri 2
  ↓
Rust
  ├── FFmpeg
  │     ↓
  │  音视频解码 / 提取音轨
  │
  └── sherpa-onnx
        ↓
  SenseVoiceSmall INT8
        ↓
  识别结果
        ↓
  Rust 统一数据结构
        ↓
  React 渲染
```

---

# 4. 技术组件职责

| 组件 | 职责 |
|---|---|
| Tauri 2 | 桌面窗口、系统能力、前后端通信、资源打包 |
| React | 页面、组件、交互、状态展示 |
| TypeScript | 前端类型约束 |
| WebView2 | Windows 下渲染 React UI |
| Tailwind CSS | UI 样式实现 |
| shadcn/ui | 基础组件骨架 |
| Lucide Icons | 统一图标 |
| Rust | 文件、FFmpeg、ASR、导出等本地逻辑 |
| FFmpeg | 视频抽取音频、音频转码 |
| sherpa-onnx | 本地 ASR 推理运行时 |
| SenseVoiceSmall INT8 | 语音识别模型 |

---

# 5. 技术原则

## 5.1 UI 与本地能力分离

React 只负责：

```text
页面
交互
视觉状态
结果展示
```

Rust 负责：

```text
文件访问
FFmpeg
模型加载
模型推理
导出文件
任务取消
```

React 不直接处理：

- FFmpeg
- 模型推理
- 文件系统底层逻辑

---

# 6. 核心处理链路

```text
用户拖入 meeting.mp4
        ↓
Tauri 获取文件路径
        ↓
Rust 检查文件
        ↓
FFmpeg
        ↓
转换为统一音频格式
        ↓
16kHz / Mono / PCM
        ↓
sherpa-onnx
        ↓
SenseVoiceSmall INT8
        ↓
得到文字 / 时间信息
        ↓
Rust 转换为统一 JSON
        ↓
React 展示结果
        ↓
复制 / TXT / SRT
```

---

# 7. 转写结果数据结构

统一返回格式：

```ts
interface TranscriptSegment {
  id: string;
  start: number;
  end: number;
  text: string;
}

interface TranscriptResult {
  fileName: string;
  duration: number;
  language?: string;
  segments: TranscriptSegment[];
}
```

示例：

```json
{
  "fileName": "meeting.mp4",
  "duration": 180.5,
  "segments": [
    {
      "id": "seg-001",
      "start": 0.0,
      "end": 5.3,
      "text": "今天我们讨论一下这个项目。"
    },
    {
      "id": "seg-002",
      "start": 5.3,
      "end": 12.1,
      "text": "首先看一下当前的开发进度。"
    }
  ]
}
```

React 不关心模型内部返回格式。

所有模型结果必须先在 Rust 层转换成统一结构。

---

# 8. 应用状态机

禁止使用大量无关联 Boolean，例如：

```ts
isUploading
isProcessing
isDone
isError
hasFile
```

统一使用状态机：

```ts
type AppState =
  | "empty"
  | "ready"
  | "transcribing"
  | "completed"
  | "error";
```

状态说明：

| 状态 | 含义 |
|---|---|
| empty | 尚未选择文件 |
| ready | 已选择文件，可以开始 |
| transcribing | 正在处理 |
| completed | 转写完成 |
| error | 任务失败 |

---

# 9. Tauri Command 设计

React 只调用有限的本地接口。

建议：

```rust
select_file()
get_media_info(path)
transcribe_file(path)
cancel_transcription()
export_txt(data, path)
export_srt(data, path)
```

React 示例：

```ts
const result = await invoke<TranscriptResult>("transcribe_file", {
  path: filePath,
});
```

---

# 10. 资源打包

所有运行依赖随程序发布。

```text
app
├── ffmpeg.exe
├── sherpa-onnx runtime
└── SenseVoiceSmall INT8
```

用户不需要：

```text
下载模型
安装 FFmpeg
安装 Python
配置环境变量
填写 API Key
```

---

# 11. 项目目录

```text
transcribe/
├── src/
│   ├── components/
│   │   ├── AppHeader.tsx
│   │   ├── DropZone.tsx
│   │   ├── FileItem.tsx
│   │   ├── TranscribeProgress.tsx
│   │   ├── AudioPlayer.tsx
│   │   ├── TranscriptList.tsx
│   │   ├── TranscriptItem.tsx
│   │   ├── BottomActionBar.tsx
│   │   ├── EmptyState.tsx
│   │   └── ErrorState.tsx
│   │
│   ├── hooks/
│   │   └── useTranscription.ts
│   │
│   ├── lib/
│   │   ├── tauri.ts
│   │   └── format.ts
│   │
│   ├── types/
│   │   └── transcript.ts
│   │
│   ├── pages/
│   │   └── Home.tsx
│   │
│   ├── styles/
│   │   └── globals.css
│   │
│   └── App.tsx
│
├── src-tauri/
│   ├── src/
│   │   ├── main.rs
│   │   ├── commands.rs
│   │   ├── media.rs
│   │   ├── ffmpeg.rs
│   │   ├── transcribe.rs
│   │   └── export.rs
│   │
│   ├── resources/
│   │   ├── ffmpeg/
│   │   ├── sherpa-onnx/
│   │   └── sensevoice/
│   │
│   └── tauri.conf.json
│
├── UI_DESIGN_SPEC.md
├── package.json
└── README.md
```

---

# 12. UI 总体风格

UI 必须严格遵守 `UI_DESIGN_SPEC.md`。

整体风格：

```text
现代极简
轻量
柔和
精致
低干扰
桌面工具感
```

视觉目标：

```text
音乐类产品的精致感
+
独立桌面工具的简洁
+
生产力工具的信息效率
```

不要设计成：

```text
传统 Windows 工具
SaaS Dashboard
AI 科技风后台
音乐播放器
```

---

# 13. UI Design Tokens

## 13.1 Colors

```css
:root {
  --color-primary: #4353FF;
  --color-primary-soft: #EEF0FF;

  --color-accent: #FF6B6B;
  --color-accent-soft: #FFAAA5;

  --color-bg: #F7F8FC;
  --color-surface: #FFFFFF;
  --color-surface-soft: #EEF0FF;

  --color-text-primary: #111111;
  --color-text-secondary: #6F7785;
  --color-text-tertiary: #9AA3B2;

  --color-border: #E5E5EA;
  --color-divider: #F0F0F5;

  --color-success: #22A06B;
  --color-warning: #E59B18;
  --color-error: #D94F4F;
}
```

### 使用原则

- `#4353FF`：主按钮、主进度、当前时间戳等关键交互
- `#EEF0FF`：弱背景、选中态、文件图标背景
- `#FF6B6B`：风险、删除、错误强调
- `#111111`：主文本
- `#9AA3B2`：辅助文字
- `#FFFFFF`：核心内容区域

禁止自行增加新的品牌色。

---

# 14. 圆角体系

```css
--radius-sm: 8px;
--radius-md: 12px;
--radius-lg: 16px;
--radius-xl: 24px;
--radius-round: 999px;
```

规则：

| 场景 | 圆角 |
|---|---:|
| 小标签 | 8px |
| 输入框 / Button | 12px |
| 文件卡片 / 播放器 | 16px |
| 大容器 | 24px |
| 圆形按钮 | 999px |

禁止自定义：

```text
10px
14px
18px
22px
```

除非规范明确允许。

---

# 15. 间距体系

以 4px 为基准：

```text
4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48
```

推荐：

| 场景 | 间距 |
|---|---:|
| 图标与文字 | 8 / 12px |
| 控件内部 | 12 / 16px |
| 卡片内部 | 20 / 24px |
| 模块间 | 24 / 32px |
| 页面边距 | 32 / 40px |

禁止无规律的：

```text
13px
19px
27px
```

---

# 16. 字体

字体：

```css
font-family:
  "SF Pro Display",
  Inter,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "Microsoft YaHei",
  Arial,
  sans-serif;
```

数字 / 时间：

```css
font-family: "SF Mono", Consolas, monospace;
```

字号：

| 场景 | 字号 | 字重 |
|---|---:|---:|
| 页面标题 | 24px | 700 |
| 区块标题 | 18px | 600 |
| 文件名 | 14px | 600 |
| 正文 | 14px | 400 |
| Button | 14px | 600 |
| 时间戳 | 12px | 500 |
| 辅助信息 | 12px | 400 |

禁止营销型大标题。

---

# 17. 阴影

默认依靠：

```text
背景
+
边框
+
留白
```

形成层级。

Token：

```css
--shadow-sm: 0 2px 12px rgba(0,0,0,0.06);
--shadow-md: 0 12px 36px rgba(17,17,17,0.10);
--shadow-primary: 0 16px 36px rgba(67,83,255,0.20);
```

规则：

- 普通卡片：无阴影
- Hover / Active：允许 `shadow-sm`
- Modal：`shadow-md`
- 主色强阴影仅用于极少数视觉焦点

禁止：

- 发光
- 重阴影
- 多层阴影

---

# 18. 页面结构

采用单窗口。

```text
┌──────────────────────────────────────────────────────────┐
│ App Header                                               │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Upload / File Summary                                   │
│                                                          │
│  Progress / Player                                       │
│                                                          │
│  Transcript Result                                       │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ Bottom Action Bar                                        │
└──────────────────────────────────────────────────────────┘
```

禁止：

- 复杂 Sidebar
- 多栏后台布局
- Dashboard
- 多页面导航体系

---

# 19. Header

高度：

```text
56–64px
```

内容：

```text
左：产品名称 / Logo
右：设置
```

样式：

```css
background: #FFFFFF;
border-bottom: 1px solid #F0F0F5;
```

默认不使用阴影。

---

# 20. Upload DropZone

默认状态：

```text
┌──────────────────────────────────────┐
│                                      │
│              ↑                       │
│       拖入视频或录音                 │
│       或点击选择文件                 │
│                                      │
│ MP4 · MOV · MKV · MP3 · M4A · WAV    │
│                                      │
└──────────────────────────────────────┘
```

样式：

```css
background: #FFFFFF;
border: 2px dashed #C9CBE0;
border-radius: 16px;
padding: 32px;
```

图标：

```text
48 × 48
圆形
#4353FF
白色 icon
```

Hover：

```css
border-color: #4353FF;
background: #F8F9FF;
```

Drag Over：

```css
border-color: #4353FF;
background: #EEF0FF;
transform: scale(1.01);
transition: 150ms ease;
```

---

# 21. File Item

```text
[图标] meeting.mp4
       128 MB · 01:24:32
                         [×]
```

规范：

```text
背景       #FFFFFF
边框       1px #EEF0FF
圆角       12px
padding    12px 14px
```

文件图标：

```text
38 × 38
圆角 10px
背景 #EEF0FF
```

文件名：

```text
13–14px / 600
```

辅助信息：

```text
12px / #9AA3B2
```

---

# 22. Button

## Primary

```css
background: #4353FF;
color: #FFFFFF;
border-radius: 12px;
height: 40px;
padding: 0 20px;
font-size: 14px;
font-weight: 600;
```

用于：

- 开始转写
- 确认操作

同一视图只允许一个最强 Primary Action。

## Secondary

```css
background: #FFFFFF;
color: #111111;
border: 1px solid #E5E5EA;
```

用于：

- 选择文件
- 复制全文
- 导出

## Danger

```text
#FF6B6B
```

只用于删除和不可逆操作。

---

# 23. 转写进度

结构：

```text
正在转写 meeting.mp4                     38%

▂▅▃▆▂▇▃▅▂▆▃▇▂▅

██████████████░░░░░░░░░

已处理 08:12 / 21:35
```

Progress：

```css
height: 4px;
track: #E5E5EA;
fill: #4353FF;
```

波形：

```text
未处理：#C9CBE0
已处理：#4353FF
高度：28–32px
```

波形只是辅助信息，不成为页面视觉主体。

---

# 24. Audio Player

播放器定位：

> 转写结果的辅助导航工具。

结构：

```text
[▶] 00:32 ━━━━━━━━━━━━━━━ 12:48  🔊
```

规范：

```text
高度 48–56px
白色背景
1px #E5E5EA
圆角 16px
```

主播放按钮：

```text
#4353FF
```

其他按钮：

```text
白底描边
```

---

# 25. Transcript

推荐结构：

```text
00:03   今天我们讨论一下这个项目。

00:12   第一部分是当前开发进度。

00:27   第二部分讨论后续安排。
```

规范：

```text
正文       14px #111111
时间戳     12px #4353FF
行高       1.7
Item padding 12–16px
分割线     #F0F0F5
```

时间戳固定宽度：

```text
56–64px
```

Hover：

```css
background: #F8F9FF;
```

当前播放段：

```css
background: #EEF0FF;
border-radius: 12px;
```

点击时间戳：

```text
跳转对应播放位置
+
激活该段
```

---

# 26. Empty State

```text
暂无转写内容

拖入一个视频或录音开始转写

[选择文件]
```

只允许：

```text
一个图标
一句主文案
一句说明
一个主操作
```

禁止大插画。

---

# 27. Error State

```text
⚠ 无法读取该文件

文件可能损坏或格式不受支持。

[重新选择]
```

规则：

- 不整块使用红色背景
- 图标 / 标题使用 `#D94F4F`
- 浅红背景只用于局部提示

---

# 28. Modal

```css
background: #FFFFFF;
border-radius: 18px;
padding: 24px;
box-shadow: 0 12px 36px rgba(17,17,17,0.10);
```

尺寸：

```text
确认类：360–420px
设置类：480–560px
```

按钮：

```text
右对齐
Primary 位于最右侧
gap 12px
```

---

# 29. Toast

```css
background: #111111;
color: #FFFFFF;
border-radius: 12px;
padding: 12px 16px;
```

用于：

```text
已复制全文
TXT 导出成功
SRT 导出成功
```

建议显示：

```text
1800–2400ms
```

严重错误不要只使用 Toast。

---

# 30. Icon

只使用：

```text
Lucide Icons
```

规范：

```text
16 / 18 / 20px
统一 stroke-width
```

禁止：

- Emoji 作为正式 UI Icon
- 多套 Icon Library 混用

---

# 31. 动效

默认：

```css
transition: 150ms ease;
```

适用于：

- Hover
- Button
- Focus
- Drag Over
- Toggle
- Transcript Active

禁止：

- 弹跳
- 大幅位移
- 强缩放
- 长时间动画
- 装饰性循环动画

Spinner / 转写波形除外。

---

# 32. 组件状态

核心组件必须实现：

```text
Default
Hover
Active
Focus
Disabled
Loading
Error
```

Disabled：

```css
opacity: 0.45;
cursor: not-allowed;
```

Loading Button：

```text
Spinner + 正在转写
```

按钮宽度必须保持稳定，避免布局抖动。

---

# 33. 桌面窗口

默认：

```text
1080 × 720
```

最小：

```text
820 × 560
```

内容最大宽度：

```text
960–1040px
```

宽度 < 860px：

```text
页面 padding → 24px
双栏 → 单栏
Bottom Action 自适应
优先保留 Transcript 阅读面积
```

---

# 34. Accessibility

必须：

- Focus 可见
- 所有按钮支持键盘
- Icon Button 有 `aria-label`
- 拖拽必须同时支持点击选择
- 状态不能只依赖颜色
- 正文字号 ≥ 12px
- 确保文本对比度

---

# 35. UI Do / Don't

## Do

- 大面积留白
- 使用稳定的圆角体系
- 主色只用于关键动作
- 使用浅灰 / 薰衣草灰区分层次
- 页面只强调一个主任务
- 优先保证 Transcript 阅读效率

## Don't

- 不做渐变背景
- 不做玻璃拟态
- 不做科技发光
- 不做 Card 套 Card
- 不做重阴影
- 不做 KPI Dashboard
- 不做复杂 Sidebar
- 不做多彩标签体系
- 不使用多个强主按钮
- 不为了装饰降低信息密度

---

# 36. Codex UI 开发硬约束

Codex 修改 UI 时必须遵守：

```text
严格遵守 UI_DESIGN_SPEC.md。

禁止自行新增：
- 品牌色
- 字体
- 圆角体系
- 阴影体系
- 页面导航
- 新的组件视觉语言
```

新增组件时：

```text
1. 优先复用已有组件
2. 使用现有 Design Tokens
3. spacing 使用 4px 基准
4. 圆角从 8 / 12 / 16 / 24 选择
5. 必须覆盖状态
6. 不增加新的视觉风格
```

修改 UI 前必须先判断：

```text
是否真的需要这个元素？
是否可以复用已有组件？
是否增加用户理解成本？
是否破坏单任务工具的简洁？
```

---

# 37. 开发阶段

## Phase 1：跑通模型链路

目标：

```text
音频
↓
FFmpeg
↓
SenseVoice
↓
终端输出文字
```

暂时不做 UI。

验收：

- 能处理 WAV
- 能输出中文
- 能稳定重复运行

---

## Phase 2：视频 / 音频输入

增加：

```text
MP4
MOV
MKV
MP3
M4A
WAV
```

统一通过 FFmpeg 转换。

验收：

- 常用格式均能转写
- 临时文件能正确清理
- 错误文件不会导致 App 崩溃

---

## Phase 3：Tauri 前后端通信

```text
React
↓
invoke()
↓
Rust
↓
Transcribe
↓
React
```

验收：

- UI 能提交文件
- Rust 能返回结果
- 错误能够返回 UI

---

## Phase 4：完成主界面

实现：

- Header
- DropZone
- File Item
- Start Button
- Progress
- Transcript
- Empty
- Error

必须完全遵守 UI Design Tokens。

---

## Phase 5：播放器

实现：

- Play
- Pause
- Seek
- 时间显示
- Transcript 时间戳跳转

---

## Phase 6：导出

实现：

```text
复制全文
TXT
SRT
```

验收：

- 中文编码正常
- SRT 时间格式正确
- 文件名合理

---

## Phase 7：打包

所有资源内置。

最终用户流程：

```text
安装
↓
打开
↓
拖入文件
↓
开始转写
↓
查看结果
↓
复制 / 导出
```

不能出现：

```text
安装依赖
下载模型
配置环境
配置 API
命令行
```

---

# 38. MVP 验收标准

满足以下完整路径即完成第一版：

```text
1. 用户安装 Windows 软件
2. 双击打开
3. 拖入 MP4 / MP3 / M4A / WAV
4. 软件正确识别文件
5. 点击开始转写
6. 显示明确进度
7. 完成后展示带时间戳文本
8. 点击时间戳能够跳转播放
9. 可以复制全文
10. 可以导出 TXT
11. 可以导出 SRT
```

同时必须满足：

```text
不联网
不登录
不选模型
不下载模型
不配置参数
不安装额外依赖
```

---

# 39. 最终开发原则

整个项目始终遵循四条：

```text
1. 简单优先于功能丰富
2. 稳定优先于可扩展
3. 固定方案优先于让用户配置
4. UI 一致性优先于单页面“自由发挥”
```

最终产品目标不是成为一个功能完整的 AI 音频平台，而是：

> 一个打开就知道怎么用、拖进去就能转、几年后仍然可以直接运行的小工具。
